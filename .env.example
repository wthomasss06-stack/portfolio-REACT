# Copier en .env à la racine pour tester /api/contact en local avec `vercel dev`
# (ou `npx vercel env pull .env` si le projet est déjà lié à ton compte Vercel)

RESEND_API_KEY=
FROM_EMAIL=onboarding@resend.dev
ADMIN_EMAIL=wthomasss06@gmail.com
